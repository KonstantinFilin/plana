{% set elErrorName = elName | replace({ '[': '.', ']': ''}) %}
{% set hasErrors = errors.all() | length > 0 %}

<div class="row mb-3">
    <label class="col-sm-2 col-form-label">{{ elLabel }}</label>
    <div class="col-sm-10">
        {{ labelContent | raw }}
    </div>
</div>
